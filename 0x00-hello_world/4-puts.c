#include <stdio.h>

/**
* main - Entry point
*
* Description: 'Using puts to display a string'
*
* Return: Always 0 (Success)
*/

int main(void)
{
	char myString[] = "\"Programming is like building a multilingual puzzle\n";

	puts(myString);

	return (0);
}
