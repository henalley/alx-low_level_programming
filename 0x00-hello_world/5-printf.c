#include < stdio.h >

/**
 * main - Entry point
 *
 * Description: 'Printing a statement using printf'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char myString[] = "with proper grammar, but the outcome is a piece of art,";

	printf(myString);

	return (0);
}
